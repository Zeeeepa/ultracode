# Suppress peer dependency warnings for tree-sitter grammars
# They have outdated peerDependencies (^0.21-0.22) while we use 0.25
# This is safe - the API is backwards compatible
legacy-peer-deps=true
