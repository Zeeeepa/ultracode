# Filter Test

Testing metadata filter for AutoDoc semantic search to ensure only documentation is returned.

## Implementation

Vector embeddings with metadata type filtering for precise AutoDoc-only search results.